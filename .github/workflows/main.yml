name: Run tests

on: [push, workflow_dispatch]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: CarbonSmasher/packtest_runner@dde9fe4542722e214e37dd03a9730542801ddd82
        with:
          packs: example-datapack.zip
