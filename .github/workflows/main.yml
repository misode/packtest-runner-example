name: Run tests

on: [push, workflow_dispatch]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: CarbonSmasher/packtest_runner@3d189498742f1c0340c586f1c0a7a69de70811d6
        with:
          packs: example-datapack.zip
