name: Run tests

on: [push, workflow_dispatch]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: CarbonSmasher/packtest_runner@6aeafa9a9c087ff981c4747065d1908368039f79
        with:
          packs: example-datapack.zip
