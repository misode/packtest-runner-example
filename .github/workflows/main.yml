name: Run tests

on: [push, workflow_dispatch]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: CarbonSmasher/packtest_runner@47e645090157cb198dcd147da3153d2d1f1474f4
        with:
          packs: example-datapack.zip
