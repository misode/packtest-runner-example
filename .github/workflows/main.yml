name: Run tests

on: [push, workflow_dispatch]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: CarbonSmasher/packtest_runner@4f5223a6b897c748f83d17952dc16264434b2b76
        with:
          packs: 'failing,succeeding.zip'
