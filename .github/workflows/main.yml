name: Run tests

on: [push, workflow_dispatch]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: CarbonSmasher/packtest_runner@a07563c8c91273239d43f0dae2c12208ee5e6a38
        with:
          packs: example-datapack.zip
